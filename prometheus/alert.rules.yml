groups:
  - name: web_alerts
    rules:
      - alert: HighRequestRate
        expr: rate(request_count[1m]) > 5
        for: 30s
        labels:
          severity: warning
        annotations:
          summary: "High request rate on web service"
          description: "request rate (per second) is > 5 for more than 30s"
